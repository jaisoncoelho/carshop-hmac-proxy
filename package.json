{
  "name": "express-hmac-proxy",
  "version": "1.0.0",
  "description": "Express API proxy that adds HMAC-SHA256 signatures to forwarded requests",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest"
  },
  "keywords": ["express", "proxy", "hmac", "aws"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "@aws-sdk/client-secrets-manager": "^3.490.0",
    "@aws-sdk/client-lambda": "^3.490.0",
    "axios": "^1.6.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.1.3"
  }
}

